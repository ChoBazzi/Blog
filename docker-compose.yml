services:
  portfolio:
    build:
      context: .
    container_name: portfolio-blog
    restart: unless-stopped
    ports:
      - "3000:3000"
    environment:
      NODE_ENV: production
      NEXT_TELEMETRY_DISABLED: "1"
